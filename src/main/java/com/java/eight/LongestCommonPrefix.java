package com.java.eight;

import java.util.Comparator;
import java.util.List;
import java.util.stream.Collectors;

public class LongestCommonPrefix {

  public static void main(String[] args) {

//    List<String> stringList = List.of("Flyer", "Flew", "Flight", "Flow", "Flavour", "Flatim");
//    String result = "";
//
//    List<String> sortedStringList = stringList.stream()
////                                              .sorted()
//                                              .sorted(Comparator.comparing(String::length))
//                                              .collect(Collectors.toList());
//    Integer size = sortedStringList.get(0).length();
//
//    System.out.println("$$$$$$$$$$$$"+sortedStringList);
//
//    char[] firstElementToChArr = sortedStringList.get(0).toCharArray();
//
//    for (int i=0; i<size; i++) {
//      int j = i;
//      boolean flag = false;
//      if (sortedStringList.stream().allMatch(e -> e.charAt(j) == firstElementToChArr[j])) {
//        result = result + firstElementToChArr[j];
//        flag = true;
//      }
//      if(!flag)
//        break;
//    }
//    System.out.println(result);

  }
}
